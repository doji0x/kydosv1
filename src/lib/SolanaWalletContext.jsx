import React,{createContext,useCallback,useContext,useMemo,useState}from'react';const Context=createContext(null);export function SolanaWalletProvider({children}){const[publicKey,setPublicKey]=useState(null),phantom=typeof window!=='undefined'&&window.solana?.isPhantom?window.solana:null;const connect=useCallback(async()=>{if(!phantom)throw new Error('Install Phantom to continue');const r=await phantom.connect();setPublicKey(r.publicKey);return r.publicKey},[phantom]);const disconnect=useCallback(async()=>{await phantom?.disconnect();setPublicKey(null)},[phantom]);const value=useMemo(()=>({installed:!!phantom,connected:!!publicKey,publicKey,connect,disconnect,signTransaction:tx=>phantom.signTransaction(tx),signAllTransactions:txs=>phantom.signAllTransactions(txs)}),[phantom,publicKey,connect,disconnect]);return <Context.Provider value={value}>{children}</Context.Provider>}export function useSolanaWallet(){const v=useContext(Context);if(!v)throw new Error('SolanaWalletProvider is missing');return v}
