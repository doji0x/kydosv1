import React from 'react';
import { ShieldAlert } from 'lucide-react';
import { Button } from '@/components/ui/button';
export default function AstraAuditCard({issue,busy,onDecision}){if(issue.supersededBy)return null;return <section className="rounded-xl border border-primary/30 bg-primary/5 p-4"><div className="flex items-center gap-2 text-primary"><ShieldAlert className="h-4 w-4"/><strong className="text-xs uppercase tracking-wider">{issue.severity} audit finding</strong></div><p className="mt-3 text-sm font-medium">{issue.finding}</p><p className="mt-2 text-sm text-muted-foreground">{issue.proposedFix}</p><p className="mt-2 font-mono text-xs text-muted-foreground">{issue.branch} · {issue.filePaths?.join(', ')}</p>{issue.status==='pending'&&<div className="mt-4 flex gap-2"><Button size="sm" disabled={busy} onClick={()=>onDecision(issue.id,'approve')}>Approve</Button><Button size="sm" variant="outline" disabled={busy} onClick={()=>onDecision(issue.id,'reject')}>Reject</Button></div>} {issue.status!=='pending'&&<p className="mt-3 text-xs uppercase text-muted-foreground">{issue.status}{issue.runStatus?` · ${issue.runStatus}`:''}</p>}</section>}