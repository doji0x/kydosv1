// Shared by manager and specialists; Git delivery is not Base44 deployment.
export const ASTRA_DELIVERY_POLICY = `Astra specialists share astra/latest as the explicit delivery branch. Avoid per-task branches. Before resuming, inspect current repository state, AGENTS.md, relevant checkpoints and upstream evidence; continue only unfinished work and re-read relevant files before editing. Do not assume earlier assistant claims establish current SHAs, finished work or active jobs. Coordinate overlapping file ownership and stop on unexpected changes rather than overwriting them.
Other agents use separate branches/worktrees. If isolation is necessary, record its owner, purpose and base SHA; safe integration into astra/latest is part of task completion, not a manual owner chore. Integrate only verified, finished work intended for this build after checking its exact commit SHA, changed files, concise handoff and focused checks. Recheck the delivery tip, preserve unrelated work, run focused checks on the integrated result and record the resulting astra/latest commit SHA. Ambiguous ownership, conflicts, failed checks or unavailable integration tools mean blocked delivery, not permission to guess or claim completion. Preserve unrelated branches and active work. Never force merges or ref updates, reset working trees, discard changes, or interrupt running jobs. Never automatically cancel jobs as branch housekeeping.
Treat another agent's main-branch changes as upstream evidence only; require its commit SHA, changed files and concise handoff before assigning dependent work. End each specialist handoff with scope, changed files, source/base and resulting delivery SHAs, checks actually run and results, blockers and next owner. If inspection, execution or deployed Base44 configuration is inaccessible, state the exact limitation. A Git commit does not prove deployed orchestration changed. Do not deploy without a separate explicit request.`;
