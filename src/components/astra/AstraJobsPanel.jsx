import React from 'react';
import useAstraJobLog from '@/hooks/useAstraJobLog';
import AstraJobLogRow from './AstraJobLogRow';
function Group({title,jobs}){return <section><h3 className="mb-2 font-mono text-xs uppercase tracking-widest text-muted-foreground">{title} · {jobs.length}</h3><div className="space-y-3">{jobs.map(job=><AstraJobLogRow key={job.id} job={job}/>)}</div></section>}
export default function AstraJobsPanel(){const {jobs,loading}=useAstraJobLog();if(loading)return <p className="text-sm text-muted-foreground">Loading jobs…</p>;if(!jobs.length)return <p className="text-sm text-muted-foreground">No specialist jobs yet.</p>;const running=jobs.filter(x=>x.visibleState==='running'),pending=jobs.filter(x=>['queued','blocked'].includes(x.visibleState)),recent=jobs.filter(x=>!['running','queued','blocked'].includes(x.visibleState));return <div className="space-y-6"><Group title="Running" jobs={running}/><Group title="Pending" jobs={pending}/><Group title="Recent" jobs={recent}/></div>}