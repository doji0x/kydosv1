import { createClientFromRequest } from 'npm:@base44/sdk@0.8.48';import { secrets } from 'base44:runtime';const allowed=new Set(['getAccountInfo','getBalance','getBlockTime','getSignatureStatuses','getSignaturesForAddress','getTokenAccountBalance']);export default async function(req:Request):Promise<Response>{try{const base44=createClientFromRequest(req);const user=await base44.auth.me();if(!user)return Response.json({error:'Unauthorized'},{status:401});const input=await req.json(),method=String(input.method||'');if(!allowed.has(method))return Response.json({error:'RPC method not allowed'},{status:400});const endpoint=secrets.get('HELIUS_RPC_URL');if(!endpoint)return Response.json({error:'Helius RPC is not configured'},{status:503});const response=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params:Array.isArray(input.params)?input.params.slice(0,5):[]})}),data=await response.json();return Response.json(data,{status:response.ok?200:502})}catch(error){return Response.json({error:error.message},{status:500})}}
